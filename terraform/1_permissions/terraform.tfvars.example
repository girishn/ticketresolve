# Copy to terraform.tfvars and set your values.
# terraform.tfvars is in .gitignore; do not commit.

aws_region      = "ap-southeast-2"
iam_user_name   = "ticketresolve-dev"
iam_policy_name = "TicketResolveDevPolicy"
