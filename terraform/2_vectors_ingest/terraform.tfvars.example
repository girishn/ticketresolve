# Copy to terraform.tfvars and set your values.
# terraform.tfvars is in .gitignore; do not commit secrets.

aws_region  = "ap-southeast-2"
bucket_name = "ticketresolve-vectors-123456789012"  # Replace with your AWS account ID or a unique suffix
